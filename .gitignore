
target/
dbt_packages/
logs/
# Raw data — never commit
data/raw/*

# DuckDB database file — never commit
dev.duckdb
*.duckdb

# Any other big files
*.csv
*.json
*.zip
# Raw data and database — never commit
data/raw/
dev.duckdb
scripts/dev.duckdb
*.duckdb
